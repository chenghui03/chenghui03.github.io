---
{"publish":true,"created":"2025-10-30T21:45:40.000+08:00","modified":"2025-10-30T22:38:42.029+08:00","cssclasses":""}
---

矩阵运算 $A_{i,j} B_{j,k} = C_{i,k}$ 共包含ik个元素都需要j次乘法和j-1次加法运算 
复杂度为
$$O(i k (2j-1)) = O(ijk)
$$

对于矩阵连乘
$$
A_1 A_2 \dots A_n 
$$
根据矩阵结合律，可以任意结合两个相邻矩阵进行计算，不同结合方式的运算时间复杂度各不相同。
例如

$$
\begin{align*}
(A_{i,j} B_{j,k}) C_{k,j} \\ O() = ijk \times ikl = i^2jk^2l\\
(A_{i,j} (B_{j,k} C_{k,j})) \\ O() = jkl \times ijl = ij^2kl^2 \\
\end{align*}
$$

考虑结合方式，当还剩下n项时候共有n-1种选择，然后还剩下n-1项，共有 $(n-1)!$ 种结合方式。

希望找到结合方式使得运算量最小。

拆分子问题
记矩阵形状
$$
(i,j)(j,k)\dots = (i,j,k, \dots)
$$
递归
```python
shape_list = [10, 20, 30, 10]  # 3个矩阵：A(10×20), B(20×30), C(30×10)

def minNumMul(i, j):
    # [i, j) 左闭右开区间
    num_matrices = j - i
    
    # 基础情况1：空区间或单个矩阵，代价为0
    if num_matrices <= 1:
        return 0
    
    # 基础情况2：两个矩阵，直接计算代价
    if num_matrices == 2:
        # 区间 [i, i+2) 包含矩阵 i 和 i+1
        # 矩阵i的维度是 shape_list[i] × shape_list[i+1]
        # 矩阵i+1的维度是 shape_list[i+1] × shape_list[i+2]
        # 它们相乘的代价是？
        return shape_list[i] * shape_list[i+1] * shape_list[i+2]
    
    numMul_list = []
    # k 的取值范围：从 i+1 到 j-1
    # 因为切分点k要把 [i, j) 分成 [i, k) 和 [k, j)
    # 两个子区间都不能为空
    for k in range(i + 1, j):
        left_cost = minNumMul(i, k)    # 左子链 [i, k) 的代价
        right_cost = minNumMul(k, j)   # 右子链 [k, j) 的代价
        
        # 合并代价：左结果维度 shape_list[i] × shape_list[k]
        #           右结果维度 shape_list[k] × shape_list[j]
        merge_cost = shape_list[i] * shape_list[k] * shape_list[j]
        
        total_cost = left_cost + right_cost + merge_cost
        numMul_list.append(total_cost)
    
    return min(numMul_list)

# 测试整个矩阵链 [0, 3)
result = minNumMul(0, 3)
print(f"最小乘法次数: {result}")
```

动态规划

